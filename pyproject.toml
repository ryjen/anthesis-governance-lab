[project]
name = "anthesis-governance-lab"
version = "0.1.0"
description = "Safe test repository for Anthesis governance scenarios"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
